defaults: &defaults
  exam:
    question_type:
      single_choice: "single choice"
      multiple_choice: "multiple choice"
      text: "text"
    state:
      finish: "Finish"
  status:
    start: "start"
    testing: "testing"
    checked: "checked"
    unchecked: "unchecked"
  questions:
    state:
      waiting: "waiting"
      accepted: "accepted"
      rejected: "rejected"
  pagination:
    per_page: 25

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
